<%= form_with(model: education_directorate) do |form| %>
  <% if education_directorate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(education_directorate.errors.count, "error") %> prohibited this education_directorate from being saved:</h2>

      <ul>
        <% education_directorate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit t(:submit) %>
  </div>
<% end %>
