<%= form_with(model: school_class) do |form| %>
  <% if school_class.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(school_class.errors.count, "error") %> prohibited this school_class from being saved:</h2>

      <ul>
        <% school_class.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :school_class, style: "display: block" %>
    <%= form.text_field :school_class %>
  </div>

  <div>
    <%= form.label :school_type, style: "display: block" %>
    <%= form.collection_select :school_type_id, SchoolType.all, :id, :school_type %></td>
  </div>

  <div>
    <%= form.submit t(:submit) %>
  </div>
<% end %>
