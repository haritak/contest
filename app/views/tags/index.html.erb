<p style="color: green"><%= notice %></p>

<h1>Tags</h1>

<div id="tags" class="container">
  <% @tags.each do |tag| %>
    <div class="row">
      <%= render tag %>
    </div>
  <% end %>
</div>

<%= link_to "New tag", new_tag_path %>
